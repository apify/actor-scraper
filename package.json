{
  "name": "actor-leboncoin",
  "version": "0.0.1",
  "description": "Crawls leboncoin.fr from the search you specify, until the number of pages you want it to stop",
  "scripts": {
    "clean": "rm -rf *-scraper/node_modules *-scraper/package-lock.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint --ignore-path .gitignore */src/* */test/*",
    "lint:fix": "eslint --fix --ignore-path .gitignore */src/* */test/*"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/guillim/actor-leboncoin.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/guillim/actor-leboncoin/issues"
  },
  "homepage": "https://github.com/guillim/actor-leboncoin#readme",
  "dependencies": {
    "@apify/eslint-config": "^0.1.0",
    "eslint": "^7.5.0"
  }
}
